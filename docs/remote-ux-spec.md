# WhaleMaid · 远程控制 UX 规范（UX-001..）

> 唯一现行版。来源：ToDesk / 网易UU / 向日葵 / RustDesk / TeamViewer 官方帮助页与公开评测的**用户可见行为**（B 档公开文档），外加本项目使用者（你）的实测体验标准。
> 规则：**任何 UX/连接相关代码必须先引用本规范编号**；你指出的每一个错误，同类条目全量对照补查，不指一修一。
> 状态列：✅ 已实现 / ◐ 部分 / ❌ 未实现（诚实基线：绝大多数未实现）。

## 1. 设备身份与发现（连接逻辑核心——你指出的代表性问题）

| ID | 行为规范 | 状态 |
|---|---|---|
| UX-001 | 受控端**启动即注册**：上线自动向服务端注册（设备编号+在线状态），服务端只知"有这个进程"，用户零操作 | ✅ 插件启动即固定指纹 HTTPS 注册（编号+密码哈希+心跳），实机验证 |
| UX-002 | **设备编号**：短、好记（9 位数字级，UU/ToDesk 式），受控端生成并持久化，主界面醒目展示 | ◐ 插件有 WHALE-XXXX-XXXX 生成并持久化；受控端控制台启动指引会醒目打印编号+长期密码位置+轮换入口（宿主终端可见）；受控端 UI 展示待 DSH 扩展面 |
| UX-003 | **设备列表自动发现**：主控端登录即见同账号全部设备+实时在线状态，**全程无 IP/端口/协议字样** | ◐ Phase A 无账号：`GET /_whalemaid/devices/:id/status` 公开限速在线查询已实装（不回路由秘密）；App 首屏 = 设备管理页（记住上次设备+输入即查在线徽章）；同账号列表属 Phase B |
| UX-004 | **网络路径透明**：P2P 打洞优先、失败自动中继，用户无感知；弱网自动降级 | ❌ 只有中继（主控端已接入，全程无 IP/端口字样）；P2P 打洞属 Phase B |
| UX-005 | **临时协助**：无账号时"设备编号+临时密码"一次性连接 | ◐ 临时密码有，流程未接 |

## 2. 凭据与授权

| ID | 行为 | 状态 |
|---|---|---|
| UX-006 | 长期密码（无人值守）：受控端设一次，之后主控端凭"编号+密码"免确认连接 | ✅ /connect 编号+密码服务端匹配（限速+锁定）→ 一次性 grant → TLS 入口 → 官方 /api 全链实测通 |
| UX-007 | 临时密码：一次性/限时，用过即焚 | ◐ 生成/消费已有，UX 未接 |
| UX-008 | 密码匹配（服务端或受控端验证）+ 错误明确提示 + 防爆破限次 | ✅ 服务端匹配已实装（scrypt PHC+限速 5/min+错 5 锁 5min）；主控端明确文案（离线/编号不存在/密码错误/锁定）实测 |
| UX-009 | 被控端知情：有人连接/尝试时受控端提示（弹窗/记录），可主动断开 | ◐ 控制台级已实装：宿主终端会打印"主控端已连接（最近 20s 内 N 次授权）——有人正在远程控制本机"（心跳 connectEvents 计数）；弹窗/记录 UI 待 DSH 扩展面 |
| UX-010 | 设备授权管理：受控端可看"谁连过"、可吊销/拉黑 | ◐ 有吊销 API（自吊销/管理令牌）与密码轮换入口（POST /whalemaid/rotate-password）；"谁连过"名单 UI 待 Phase B |

## 3. 连接体验

| ID | 行为 | 状态 |
|---|---|---|
| UX-011 | **秒连预期**：点设备→输密码→连上；失败给原因与重试，永不出现 IP/端口/协议 | ◐ 移动壳已实测闭环且启动自动连接上次设备（固定本地端口+原生记住状态）；连接中状态/延迟标识仍缺 |
| UX-012 | 断线自动重连并恢复现场（会话不丢） | ◐ 受控端 rathole 指数退避重连已实装（上限 30s）；主控端重连 = 重新 /connect（grant 单次消费；密码/会话令牌自动回退），Android 壳已实测闭环 |
| UX-013 | 在线状态实时刷新（心跳驱动，秒级） | ◐ 连接页设备编号输入即查在线徽章(在线/离线/不存在, 2026-08-16 实测)；连接后设备列表实时刷新待设备管理 UI |
| UX-014 | 连接中标识：连接状态/延迟/加密标识（"已加密"） | ◐ 连接页常显"全链加密：TLS/WSS + noise、证书指纹固定"说明(实测)；连接中状态与延迟标识待设备管理 UI |

## 4. 控制台（连接后 = 移植的 DSH 原生界面，ADR-039）

| ID | 行为 | 状态 |
|---|---|---|
| UX-015 | 官方 DSH 前端移植 + 移动适配（原生壳打包） | ◐ 官方 dist vendor 已入库；Web 版实测闭环；Electron 壳 smoke 过；Capacitor Android 壳全链实测（官方 UI/API/WS 隧道+AbortSignal polyfill+记住登录态自动重连+在线徽章，2026-08-16）；iOS Swift 移植已完成，CI 构建验证中 |
| UX-016 | 审批/权限预设交互与桌面一致 | ✅ 官方前端原生自带（移植即得）；权限预设按钮与审批流经手机隧道原生渲染（实测会话内 Workspace Write 预设展示/切换） |
| UX-017 | 电脑文件夹访问（browse seam 移动 UI） | ✅ 手机端真实点击级验证：添加工作区→目录浏览器列出宿主目录树→编辑路径→新建文件夹→创建→选中→打开→宿主工作区落盘（picker 客户端面由插件补挂，2026-08-16） |
| UX-018 | 照片（相机/相册）与语音（麦克风）原生桥 | ◐ Android 已实现：相机/相册/文件选择 + 麦克风录制 API（opaque cache asset + 256 KiB 分块读取 + release；RECORD_AUDIO 权限，系统选择器实测）；官方 composer client module 接线与 iOS 对等桥待完成 |

## 5. 账号体系（Phase B，服务端闭环）

| ID | 行为 | 状态 |
|---|---|---|
| UX-019 | 同账号即绑定（UU 式）：受控端登录账号，主控端同账号自动可见；可选"仍要求设备密码"开关 | ❌ |
| UX-020 | 设备分组/改名/排序 | ❌ |

## 6. 错误与安全提示

| ID | 行为 | 状态 |
|---|---|---|
| UX-021 | 密码错误/设备离线/被吊销：各自明确文案与下一步 | ✅ Web 与 Android 壳均实现：404 设备编号不存在/503 设备不在线/401 密码错误+锁定提示（连接页与状态徽章实测） |
| UX-022 | 受控端首次启用引导：设置长期密码 + 安全说明 | ◐ 控制台级已实装：受控端启动即打印编号/长期密码位置/轮换入口/安全说明（"有人连接=完整远程控制"）；UI 级引导待 DSH 扩展面 |

## 7. 结论（现状诚实评估）

你判断得对（当时）：**差距不在代码量，在于从未把竞品用户流程当规范**——这份文档就是纠正。实现顺序：UX-001..008（连接逻辑闭环）→ UX-011..014（连接体验）→ UX-015..018（控制台）→ Phase B（UX-019/020）。
现状（2026-08-16）：Phase A 已按此顺序闭环——连接逻辑（UX-001/006/008/021 ✅）、连接体验（UX-011..014 ◐，移动壳全链实测）、控制台（UX-015..018 ◐，Android 原生桥已落、官方 UI 隧道原生渲染实测）；剩余 ◐ 项中的缺口集中在 UI 级扩展面与 Phase B 账号体系（UX-019/020 ❌ 属服务端闭环规划内）。
