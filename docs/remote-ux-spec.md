# WhaleMaid · 远程控制 UX 规范（UX-001..）

> 唯一现行版。来源：ToDesk / 网易UU / 向日葵 / RustDesk / TeamViewer 官方帮助页与公开评测的**用户可见行为**（B 档公开文档），外加本项目使用者（你）的实测体验标准。
> 规则：**任何 UX/连接相关代码必须先引用本规范编号**；你指出的每一个错误，同类条目全量对照补查，不指一修一。
> 状态列：✅ 已实现 / ◐ 部分 / ❌ 未实现（诚实基线：绝大多数未实现）。

## 1. 设备身份与发现（连接逻辑核心——你指出的代表性问题）

| ID | 行为规范 | 状态 |
|---|---|---|
| UX-001 | 受控端**启动即注册**：上线自动向服务端注册（设备编号+在线状态），服务端只知"有这个进程"，用户零操作 | ✅ 插件启动即固定指纹 HTTPS 注册（编号+密码哈希+心跳），实机验证 |
| UX-002 | **设备编号**：短、好记（9 位数字级，UU/ToDesk 式），受控端生成并持久化，主界面醒目展示 | ◐ 插件有 WHALE-XXXX-XXXX 生成，但未展示在受控端 UI |
| UX-003 | **设备列表自动发现**：主控端登录即见同账号全部设备+实时在线状态，**全程无 IP/端口/协议字样** | ◐ Phase A 无账号：`GET /_whalemaid/devices/:id/status` 公开限速在线查询已实装（不回路由秘密）；同账号列表属 Phase B；App 首屏设备列表待官方前端移植 |
| UX-004 | **网络路径透明**：P2P 打洞优先、失败自动中继，用户无感知；弱网自动降级 | ❌ 只有中继，且未接入 App |
| UX-005 | **临时协助**：无账号时"设备编号+临时密码"一次性连接 | ◐ 临时密码有，流程未接 |

## 2. 凭据与授权

| ID | 行为 | 状态 |
|---|---|---|
| UX-006 | 长期密码（无人值守）：受控端设一次，之后主控端凭"编号+密码"免确认连接 | ✅ /connect 编号+密码服务端匹配（限速+锁定）→ 一次性 grant → TLS 入口 → 官方 /api 全链实测通 |
| UX-007 | 临时密码：一次性/限时，用过即焚 | ◐ 生成/消费已有，UX 未接 |
| UX-008 | 密码匹配（服务端或受控端验证）+ 错误明确提示 + 防爆破限次 | ◐ 服务端匹配已实装（scrypt PHC+限速 5/min+错 5 锁 5min+错误 401 提示）；App 端提示文案待移植 |
| UX-009 | 被控端知情：有人连接/尝试时受控端提示（弹窗/记录），可主动断开 | ❌ |
| UX-010 | 设备授权管理：受控端可看"谁连过"、可吊销/拉黑 | ◐ 有吊销 API，无 UI/名单展示 |

## 3. 连接体验

| ID | 行为 | 状态 |
|---|---|---|
| UX-011 | **秒连预期**：点设备→输密码→连上；失败给原因与重试，永不出现 IP/端口/协议 | ◐ 服务端闭环已通（编号+密码+grant+TLS+noise，无 IP）；App 界面待官方前端移植完成 |
| UX-012 | 断线自动重连并恢复现场（会话不丢） | ◐ 受控端 rathole 客户端指数退避重连已实装；主控端重连 = 重新 /connect（grant 单次消费）待 App 落地 |
| UX-013 | 在线状态实时刷新（心跳驱动，秒级） | ◐ 45s 心跳窗口，未接 UI |
| UX-014 | 连接中标识：连接状态/延迟/加密标识（"已加密"） | ❌ |

## 4. 控制台（连接后 = 移植的 DSH 原生界面，ADR-039）

| ID | 行为 | 状态 |
|---|---|---|
| UX-015 | 官方 DSH 前端移植 + 移动适配（原生壳打包） | ◐ 官方 dist 已 vendor（47f9438 冻结+LICENSE/NOTICES）；Web 版主控端实测闭环（设备管理+官方 UI/API/WS 经隧道反代）；Electron/Capacitor 壳待包 |
| UX-016 | 审批/权限预设交互与桌面一致 | ◐ 官方前端原生自带（移植即得）；App 壳落地后按官方 UI 验收 |
| UX-017 | 电脑文件夹访问（browse seam 移动 UI） | ◐ 协议通，UI 待移植 |
| UX-018 | 照片（相机/相册）与语音（麦克风）原生桥 | ❌ |

## 5. 账号体系（Phase B，服务端闭环）

| ID | 行为 | 状态 |
|---|---|---|
| UX-019 | 同账号即绑定（UU 式）：受控端登录账号，主控端同账号自动可见；可选"仍要求设备密码"开关 | ❌ |
| UX-020 | 设备分组/改名/排序 | ❌ |

## 6. 错误与安全提示

| ID | 行为 | 状态 |
|---|---|---|
| UX-021 | 密码错误/设备离线/被吊销：各自明确文案与下一步 | ❌ |
| UX-022 | 受控端首次启用引导：设置长期密码 + 安全说明 | ❌ |

## 7. 结论（现状诚实评估）

你判断得对：**当前是玩具级**。差距不在代码量，在于从未把竞品用户流程当规范——这份文档就是纠正。实现顺序：UX-001..008（连接逻辑闭环）→ UX-011..014（连接体验）→ UX-015..018（控制台）→ Phase B（UX-019/020）。
