# 所有者指令与更正全集（OWNER DIRECTIVES）

> **长期对照文件。** 全部指令与更正按时间收录；每条带状态：✅已改正 / ◐部分 / ❌待办 / 废止。修订只改状态，不改原文摘要。
> 铁律：你指出一个错误 → 本文件同类条目全量复查，不指一修一；新代码先登记对应编号。
> 结尾 §原文留存 保留关键指令的逐字原文，无对照时以原文为准。

## 一、方向与检索（早期）

| ID | 指令/更正 | 状态 |
|---|---|---|
| D-001 | 先讨论和搜索："dpsk 用手机操作电脑，相当于远程安卓端"是否存在 | ✅ 已检索，结论：无现成；dsh-web-ui 有部分（移动端远程，但需扫码+无工作区创建） |
| D-002 | 指正：dsh 是前后端 web 形式（无需反编译）；要主动发现插件生态；只有没人发布工作才有意义 | ✅ 已发现插件生态（awesome 双列表/商店）；判定：广义手机远程已有人做，精确闭环空缺 |
| D-003 | 指正：SaaS 只是中继形态（提供服务器转接流量，散户免自建）；与 Happy 同理可自托管 | ✅ 已改为双形态（自托管/官方中继） |
| D-004 | 核心=一次验证后续安全；复制远程桌面软件（ToDesk/向日葵/UU）的安全方案 | ◐ 编号+密码+防爆破限速锁定+一次性 grant+凭据心跳已实现；LAN 拓扑复核（2026-08-17）：BlueStacks 填 `172.20.10.3:9180` 无桥接直达，全链已验。待实体手机/外网拓扑 |
| D-005 | Level 0/1/2 语义 + 手机绑定送试用 + 仅邮箱 Level -1 一周停服；**开源代码同时兼容原始 IP 连接（不亏待传统程序员）**；仓库归属 esilama | ◐ 分层/控制台已实现；IP 直连保留但**主流程应服务端发现**（见 D-026）；仓库实为 Es1lama |
| D-006 | 开源传承污染：不影响商业化可继承；影响则用洁净室或切换对照代码 | ✅ 三档判据 + 四步法已立规并执行 |
| D-007 | 命名不要 dsh 前缀；倾向女仆/仆人/贴身秘书系 | ✅ WhaleMaid（鲸娘） |
| D-008 | 主战场大陆先行；邮箱可注册但不绑手机连 level 0 都不给 | ✅ 控制台已实现（Level -1 一周停服） |
| D-009 | 视觉国内优先（DeepSeek-OCR/通义）；目标=处理一定视觉问题，非原生 MLLM | ◐ 适配器已预制，未接真实 key 实测 |
| D-010 | 热词插件 V1 发布 | ✅ 独立包已实现（规则抽取+差集上传） |
| D-011 | 文档六原则（简洁/有效/时效/指导/一一对应/溯源）；git init（esilama） | ◐ 已立规；**错误文档/代码未及时清除**（见 D-030） |
| D-012 | 工程哲学三条理论（造轮子/复用边界/传承盈利）；协议 AGPL 确认 | ✅ AGPL-3.0；**理论执行失败两次**（见 D-027） |
| D-013 | 闭源=完整控制管理系统；多端（ubuntu/windows/网页/安卓/iOS/鸿蒙），统一 API 影响小 | ◐ 控制台骨架完成；多端策略被 D-020..024 重写 |

## 二、执行与验证（中期）

| ID | 指令/更正 | 状态 |
|---|---|---|
| D-014 | 今晚跑一晚上构建（goal 化） | ✅ 完成（M0+M1 骨架+实机冒烟） |
| D-015 | PM 模式：构建细节不过问；github 双仓（public+private）建仓推送；实机/安装/最大测试我自己来；迭代后规整减负 loop | ✅ 双仓已建已推；实机测试做了 API 级+安卓模拟器级 |
| D-016 | 环境：优先本机环境、可补充安装、不操作根目录外文件夹、可用 web 或直接编译软件 | ✅ 全程沙盒内（.toolchain/.android-sdk 等） |
| D-017 | goal 定为四块代码全完成；验收=只剩注册邮箱/短信厂商、填语音/视觉 key、备服务器即可上架 | ◐ 目标不变；LAN 拓扑复核（2026-08-17）已通过，CI 全平台绿；剩实体手机外网/真机抽检（NEEDED-BY-OWNER 第 4 项）与真实 key 实测（第 5 项）后才能声明只余前三项 |
| D-018 | 实机全流程验证：用户级（真实点击）+部署者级；可用模拟器；可派 codex（注意 set proxy，多模态强）；含真实极小任务、创建工作区、调模式、看轨迹、改设置（绝对原生 dsh 体验） | ◐ 已实测：连接→官方界面→事件流→创建工作区（phone-ws）→发任务→改设置（README 记录）；调模式/看轨迹待显式复核 |
| D-019 | BlueStacks 可用；夜神驱动问题别用 | ✅ 已弃 Nox；BlueStacks 装了 APK |

## 三、架构大修正（近期，优先级最高）

| ID | 指令/更正 | 状态 |
|---|---|---|
| D-020 | 移动端（安卓/iOS）UI 与原生网页端尽量一致=**代码复用**；整个逻辑是**远程控制逻辑**（PC1 可控制 PC2）；手机无法运行 dsh | ◐ 官方前端经隧道原生渲染已实现；LAN 拓扑复核（2026-08-17）BlueStacks 直连中继 LAN 地址通过，Android 全链已验；iOS 对等桥 CI 绿但真机仍需复核 |
| D-021 | UI 与使用逻辑与 ToDesk 差距巨大；"你安卓端代码和原生 dsk 体验相差太大"；用 Kotlin 完全重构（后被 D-022 修正）；"原来的 dsh 和你的构建是完全不一样的" | ◐ 已认错；Kotlin 路线废止（D-022） |
| D-022 | 不是复刻是**前端移植后改动**：软件=背后运行逻辑+前端；手机端控制=把这个前端的接口从直连改为打到受控端 dsh | ◐ 官方前端独立构建与插件隧道载体已实现；LAN 拓扑复核（2026-08-17）Android 手机端经中继 LAN 地址直连宿主 3181，官方 UI/API/WS 隧道全链已验 |
| D-023 | 移植可直接打包成 app（不要浏览器）；移植不止移植：服务器转接、电脑文件夹访问、照片上传、语音录音（移动端原生接口） | ◐ 官方 dist 已 vendor 入库（+LICENSE/THIRD_PARTY_NOTICES/provenance）；Web 版主控端可跑（设备管理+隧道反代官方 UI，实测闭环）；Android APK 已打包实机闭环；原生桥（相机/相册/文件/麦克风录制）Android 已实现（系统选择器实测），composer 接线与 iOS 对等桥进行中 |
| D-024 | 三端实体：**受控端=dsh+插件**（任何能跑 dsh 的端）；**主控端=控制 app（无需 dsh：pc/安卓/iOS/鸿蒙）**；**服务端=流量转接**；大部分代码不变只改原生 api/sdk（照片上传、语音录音、流量发送） | ◐ 三端代码路径已实现到 Web/Electron/Android，iOS 对等桥 CI 绿；LAN 拓扑复核（2026-08-17）Android 经 LAN 中继地址全链已验，可据此声明三端路径（Android 实机 + Web + iOS 构建）成立；待实体手机外网复核 |
| D-025 | 移动端壳我调查定；PC 同时提供 Electron 和 Web 供用户选择 | ✅ 已定（Capacitor + Electron/Web） |
| D-026 | 体验细节：**不该填 IP**（ToDesk/UU 只需设备编号+密码）；受控端↔服务端注册（服务端知道有这个进程）→ 移动端↔服务端握手拿设备列表 → 账号密码服务端匹配 → 连上；质问之前让我填的 IP 是谁的 | ◐ 注册、状态、编号+密码、grant、TLS、noise 与官方界面代码路径已实现；LAN 拓扑复核（2026-08-17）App 只填编号+密码经 LAN 中继地址连接成功，注册/状态/连接/隧道全链已验；同账号设备列表仍属 Phase B |
| D-027 | 结论质疑：连接逻辑都没实现=玩具级；根因=网络安全知识不足、用户体验没考虑；违反两次铁律（①自创安卓前端 ②远控基本逻辑不通）；**搜索调查学习先于代码构建** | ◐ 已认错+立 UX 规范；改正进行中 |
| D-028 | 指一修一模式批评：指出的错误只是代表性，要主动思考其他同类错误 | ◐ 已立"同类全查"铁律，需持续执行 |
| D-029 | 体验规范次要；**不学习成熟实现的信道安全 = 肉鸡风险**；和 codex 配合不能纸上谈兵 | ✅ SEC-001（TLS+指纹固定+每设备凭据）、SEC-002（scrypt PHC+限速+锁定）、SEC-003（隧道 token 属受控端侧、/connect 不轮换）、SEC-004/004b（grant 单次消费+TLS/WSS 双入口）已修并实机验证（2026-08-16 手机隧道复核）；**信道已实测纠正：rathole 默认 transport 是 TCP 明文（源码 config.rs），现显式 noise + 静态 X25519 密钥 + 受控端 pin 公钥，双端实测通/错公钥握手失败（scripts/rathole-noise-e2e.mjs）** |
| D-030 | **错误污染源（错误文档/代码）及时 git 备份后清除**，不污染工作区；把全部指令与更正记录成一份文件长期对照改正；**设定成 goal**；结尾保留指令原文避免误解 | ◐ 本文件即该记录；本轮清仓：协议改 v2（删 /api/v1 现行位）、DESIGN §3/§5/§8/§11 改 v3、ADR-007/029/037 标废止、PREFLIGHT/threat-model/native-app-plan 同步、插件删 /m 自研 PWA 服务与构建产物、新增 CI 守卫 .github/scripts/check-banned-terms.sh 阻断废止语义回潮 |
| D-031 | **所有实现、配置、演示、测试和验收都必须按可上线、可跨设备、可规模部署的产品路径完成；任何依赖开发机、模拟器特权、调试桥、手工端口映射或隐藏前置状态的路径只能用于分段诊断，不能作为用户操作、完成状态或验收证据；不允许中间任何悄悄降级** | ✅ 中继已改 LAN 可路由监听（`*:9180`/`*:9443`）；修复 `--client` 缺失 bug（此前 rathole 子进程从未启动，隧道从未建立）；BlueStacks 手机 App 直接填 `172.20.10.3:9180`（LAN 地址、无 adb reverse）→ 中继 → noise → 宿主 3181 全链已验：官方 UI/API/WS 隧道 + V1 路由透传（2026-08-17）。CI 三连绿（无断连）。TLS 隧道 9443 + rathole noise 服务端口 5205 均实测 200 OK 返回。待实体手机/LAN 外网拓扑抽检 |
| D-032 | **受控端身份单位是加载 WhaleMaid 的准确 DSH profile/实例，不是整台电脑，也不是另一套测试 DSH；同机可有多个不同工作状态的 DSH 实例。设备编号、长期/短期密码、中继凭据、隧道路由必须绑定所选插件实例，并继承该实例自己的会话、凭据、设置、工作区、模型、权限、审批与 Codex 状态；允许一对多、多对一、多对多，但不得串实例或另开空状态 DSH** | ◐ 设备身份已由 loader `ctx.baseUrl` 绑定当前 profile（`WHALE-N2MC-43W6` 绑定 profile test，与旧全局 `WHALE-D68Z-7HBK` 分开）；同 profile 的跨进程第二 owner 会在注册前失败，同进程 HMR 可重入且 disposal 释放锁，异常退出锁可回收。长期密码/中继凭据已在 profile 独立持久化验证；临时密码生成（`POST /api/whalemaid/temporary-password`）从该 profile 实例发出 `WMT-RPKU-WZ64`，relay 按 TTL+generation 原子消费验证通过。待同机多 profile 并发状态继承复核 |

## §原文留存（逐字，无对照时以此为准）

> D-005（节选）："小提醒：开源的代码中同时兼容原始的ip连接（不要亏待传统老程序员，我们就是提供增值服务赚钱的）"
> D-006："开源传承污染是这样的：如果确认不影响商业化，那么传承可以。如果影响，就用我的方法，或者我们可以切换对照的代码。 你看着办。"
> D-011："我们做文档是为了后续工程好开发，需要时刻注意：文档简洁性、有效性、时效性（即不放旧的语义污染后续代码构建）……以及前文提到的指导性，与项目与代码的一一对应性、追查溯源可使用性。"
> D-012："我只给你理论：如造轮子理论，如复用边界条件理论，如开源传承与污染盈利理论。具体还是你后续分析。协议确认。"
> D-013："1：闭源的盈利性代码的地方应该是要有一个完善的控制管理系统。 2：这是多端的。之后还需要添加ubuntu、windows 的内容，还有网页、安卓、ios和鸿蒙……"
> D-015："构建具体细节我不多过问。当我是产品经理……github创建仓库并且推，但是开源闭源两个部分要用两个仓库……一个public一个仅自己可见。实机、与安装、最大限度的测试：你自己来。因为可能要反复迭代。迭代之后代码可能会变臃肿，那你又要进行规整和减负，这是一个loop。"
> D-016："环境问题：优先用本机已有的较完整环境，如果没有，可补充安装。其他类似问题我允许你站在我的角度思考。"
> D-017（节选）："全部都写好。包括不同主要来源的api，你先做好对应的api的预制。……你连dsh插件都没有完成。 所以这个goal太短了。你的goal定位为：四块代码全完成。我的验收是项目要到达的进度为：只能留下一些我的注册对接厂商（比如邮箱服务，短信服务）、填入语音、视觉api、拥有一台服务器可以直接上架的事情。"
> D-018（节选）："能实机跑吗？比如我电脑上有模拟器，能computer use吗？或者布置任务给codex（注意set proxy，codex多模态能力强）。先在本地跑完全流程验证……而且是用户级验证，这个用户级是指真的用户点击，还有部署者。跑全链路。……其他的基本能力（如真实通过手机端创建工作区、调整模式，看到对话和轨迹、创建会话、真实跑一个极小任务，更改设置（注意：我原先说的是绝对原生的dsh体验））"
> D-019："bluestack可用。我用它玩过游戏，夜神可能是驱动问题，别用了。"
> D-020："移动端，也就是安卓端/ios端的ui和原生网页端尽量一致，我是说，代码复用。而且整个逻辑应该是远程控制逻辑。也就是PC1是可以控制PC2的，或者反过来。手机无法运行dsh所以没这回事"
> D-021："这个ui，太不符合我的要求了。还有使用逻辑。和todesk差别也巨大。 先不着急调试代码。我要纠正你。我可以说，你安卓端代码，和原生dpsk体验，相差太大了。你用korin完全重构。但是原来的dsh和你的构建是完全不一样的。可以理解吗？"
> D-022（节选）："阶段b仍然有问题。我更希望不是复刻，而是前端移植后改动。一个软件我这么理解。一个是背后运行逻辑，一个是前端。……那么手机端控制，后端是将这个前端的接口，由直接运行，变为链接电脑的受控dsh。"
> D-023（节选）："1：前端移植，可以直接打包成app吗？我不希望还是打开一个浏览器。 2：你能够理解所谓移植，但是不止移植吗？因为我们还要与服务器等内容进行转接，还有提供电脑的文件夹访问之类的内容。这是超出直接复制粘贴的地方。"
> D-024（节选）："我们将逻辑这样区分。受控端：dsh+插件， 主控端：无需dsh，是控制app，服务端：流量转接。 受控端为可以运行dsh的任何端（如果哪天安卓也可以运行了，完全可以，软件连改都不用改，因为我们是插件）……主控端是任何安装控制app的端口，比如pc、安卓、ios、甚至鸿蒙。 大部分代码都不变，只是对于原生的一些接口要改部分的api/sdk（主要涉及到我说的那个后端流量发送的步骤，还有照片上传问题，语音录音的移动端接口）。"
> D-025："1. 移动设备你调查后觉得可以就可以，PC就electron 和 web都提供，让用户选择。"
> D-026（节选）："我感觉你基本理解了。但是还需要提醒的是，之前你和我保证的所谓模仿todesk，完成的结果也非常差……目标是：接近网易uu/todesk的体验。细微的地方，你比如说还要填写ip地址，我不知道todesk怎么做到的，但是todesk /uu只需要填写设备编号和密码即可使用，所以ip地址如果是必须的话，那感觉不方便。……受控端和服务端握手注册，服务端至少得知道有这么个进程，然后移动端和服务端握手，告诉服务端你可以选择的进程。那账号密码在服务端一匹配，成功了就给你连上。 这个过程是我猜的，但是你之前我觉得没实现桌面圆孔的逻辑，你之前让我填写ip和密码，ip是谁的？受控端还是服务端？"
> D-027："这暴露一个问题，我让你模仿远程桌面，我通过体验猜测得到的结果，你竟然没有实现？那结论是什么，你的这个软件，不是一个商业级别的软件，还是玩具级别的，很深层的原因是：网络安全知识、用户体验没考虑到。 最重要的是：我强调不要自己造轮子，除非考虑协议影响商业化。还有搜索调查学习先于代码构建。 这个问题我知道是ai的通病，但是我强调过后你犯过两次错 1：前端不用dsh，整个安卓自创 2：你连桌面远控的最基本逻辑都没通，我怎么相信你能够真的实现清楚这些需要加密信道涉及无数电脑安全的代码呢？"
> D-028："我是不是说过，我指出你的错误，不代表你只有这些错误，一般只说最具有代表性的，我不是全知全能。你连思考都没有思考过其他问题。"
> D-029："体验规范还是次要的了。你不学习人家代码，你的这种涉及到网络安全的东西，万一，有坏人信道攻击呢？无数电脑变成肉鸡？还有我说了要和codex配合，你当耳边风了？"
> D-030："你要改的东西太多了。我还要添加：错误污染源（如错误文档内容，错误代码） 及时git 备份，然后及时清除。不要污染工作区。另外我前前后后给出了非常多指令和更正，你记录一份下来，之后很长时间对着这份文件，全部改正了。设定成goal。记录的结尾要直接保留我的这好几条指令的原文，避免你误解，后续无对照"
> D-031："模拟器在我的电脑上，服务也在我的电脑上，本来就在一个局域网上，如果这样你还要专门桥接，未来推广怎么办？你要对着真正广泛推广的项目去做。不是玩具。"；"我认为必须所有都按照上线指标来做。中间任何悄悄的降级是不对的。"
